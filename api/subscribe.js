const https = require('https'); const RESEND_API_KEY = process.env.RESEND_API_KEY; module.exports = async (req, res) => { res.setHeader('Access-Control-Allow-Origin', '*'); res.setHeader('Access-Control-Allow-Methods', 'POST, OPTIONS'); res.setHeader('Access-Control-Allow-Headers', 'Content-Type'); if (req.method === 'OPTIONS') return res.status(200).end(); if (req.method !== 'POST') return res.status(405).json({ error: 'Method not allowed' }); const { email } = req.body || {}; if (!email || !email.includes('@')) return res.status(400).json({ error: 'Please enter a valid email address.' }); return new Promise((resolve) => { const data = JSON.stringify({ from: 'Jessica — Faith & Mom Life <jessica@faithandmomlife.com>', to: email.trim().toLowerCase(), subject: 'Your Free 7-Day Prayer Planner is Here!', html: '<p>Hi mama! Click to download your <a href="https://faithandmomlife.vercel.app/prayer-planner.pdf">7-Day Prayer Planner</a>. With love, Jessica</p>' }); const options = { hostname: 'api.resend.com', path: '/emails', method: 'POST', headers: { 'Authorization': 'Bearer ' + RESEND_API_KEY, 'Content-Type': 'application/json', 'Content-Length': Buffer.byteLength(data) } }; const r = https.request(options, (resp) => { let body = ''; resp.on('data', c => body += c); resp.on('end', () => { if (resp.statusCode >= 200 && resp.statusCode < 300) { res.status(200).json({ success: true }); } else { res.status(500).json({ error: 'Something went wrong.' }); } resolve(); }); }); r.on('error', () => { res.status(500).json({ error: 'Something went wrong.' }); resolve(); }); r.write(data); r.end(); }); };
